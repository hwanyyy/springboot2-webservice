<!DOCTYPE HTML>
<html>

<!--
css와 js의 위치가 다른 이유
    -> 페이징 로딩 속도를 높이기 위해 css는 header에 js는 footer에 둠, HTML은 위에서부터 코드가 실행되기 때문에 head가 다 실행되고서야 body가 실행됨
    즉, body가 다 불러지지 않으면 사용자 쪽에서는 백지 화면만 노출. 특히 js의 용량이 크면 클수록 body부분의 실행이 늦어지기 때문에 js는 body하단에 두어 화면이 다 그려진 뒤에 호출하는 것이 좋음
    반면 css는 화면을 그리는 역할이므로 head에서 불러오는 것이 좋음 그렇지 않으면 사용자가 깨진 화면을 보게 됨
    (추가로 bootstrap.js의 경우 jquery가 꼭 있어야하므로(의존) 먼저 호출)
-->
<head>
    <title>SpringBoot WebService</title>
    <meta http-equiv="Content-Type" content="text/html; charset=UTF-8" />

    <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.3.1/css/bootstrap.min.css">
</head>
<body>